declare module "tailwindcss/lib/util/flattenColorPalette" {
    // Adjust the type below based on the actual usage
    export default function flattenColorPalette(colors: unknown): Record<string, string>;
  }
